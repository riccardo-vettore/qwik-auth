import {component$} from '@builder.io/qwik';

export default component$(() => {
    return (
        <div class="flex flex-row items-center justify-center pt-16 font-bold">
            <h1>Qwik Auth</h1>
        </div>
    );
});